<Window x:Class="Edimsha.ResolutionDlg"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Edimsha"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" 
        mc:Ignorable="d"
        Title="Resoluciones usadas" 
        Height="185" 
        Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <StackPanel>
        <StackPanel.Resources>
            <Style TargetType="Label">
                <Setter Property="Height" Value="24"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Height" Value="24"/>
            </Style>
            <Style TargetType="ComboBox">
                <Setter Property="Height" Value="24"/>
            </Style>
            <Style TargetType="xctk:IntegerUpDown">
                <Setter Property="Height" Value="24"/>
            </Style>
        </StackPanel.Resources>

        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Resoluciones:" Grid.Row="0" Grid.Column="0" Height="Auto" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            <ComboBox Grid.Row="0" Grid.Column="1" Height="24" Margin="0,0,5,0"/>

            <Label Content="Ancho (px) (X):" Grid.Row="1" Grid.Column="0" Height="Auto" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            <xctk:IntegerUpDown Grid.Row="1" Grid.Column="1" Margin="0,5,5,0"/>

            <Label Content="Alto (px) (Y):" Grid.Row="2" Grid.Column="0" Height="Auto" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            <xctk:IntegerUpDown Grid.Row="2" Grid.Column="1" Margin="0,5,5,0"/>

            <Button Content="Guardar resolucion" Grid.Row="3" Grid.Column="1" Height="24" Margin="0,5,5,0"/>
        </Grid>

        <StackPanel>
            <Grid>
                <Grid.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="5,5,5,5"/>
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Content="Eliminar" Grid.Column="0" Height="24"/>
                <Button Content="Cancelar" Grid.Column="1" Height="24"/>
                <Button Content="Cargar resolucion" Grid.Column="2" Height="24"/>
            </Grid>
        </StackPanel>
    </StackPanel>
</Window>
